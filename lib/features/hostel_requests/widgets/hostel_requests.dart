import 'package:bennett_app/utils/widgets/red_app_bar_scaffold.dart';
import 'package:flutter/material.dart';

class HostelRequests extends StatelessWidget {
  const HostelRequests({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const RedAppBarScaffold(

      body: SizedBox(),
      title: 'Hostel Requests',
    );
  }
}